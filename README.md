# base-ios-app

## Starter app

```bash
cd starter-app
npm install
npx expo start # or run `npm run web`
```

```bash
npm run web
npm run android # need Android studio and emulator
npm run ios # need xcode and simulator
```
